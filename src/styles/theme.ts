const TEXT = {
  hide: `
    position: absolute;
    clip: rect(0 0 0 0);
    width: 1px;
    height: 1px;
    margin: -1px;
    overflow: hidden;
  `,
};

const COLOR = {};

const SIZE = {};

const ROUND = {};

const WIDTH = {
  pc: "1100px",
  mid: "800px",
  mobile: "480px",
};

const theme = {
  TEXT,
  COLOR,
  SIZE,
  ROUND,
  WIDTH,
};

export default theme;
